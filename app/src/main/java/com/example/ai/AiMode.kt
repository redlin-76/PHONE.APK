package com.example.ai

enum class AiMode {
    OFF,     // Pure physical phone connection (User speaks directly)
    AUTO,    // AI assist mode (AI monitors & displays suggestions, email summary helper)
    FULL_AI  // Full AI Mode (AI answers call and converses with the caller in real-time)
}
